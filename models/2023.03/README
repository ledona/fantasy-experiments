## Setup
sudo /venv/bin/pip install ipykernel

# for autosklearn
sudo apt-get install swig
sudo /venv/bin/pip install auto-sklearn


# for tpot (as of 2023.3.8)
sudo /venv/bin/pip install torch torchvision torchaudio "numpy<1.24"
sudo /venv/bin/pip install tpot
