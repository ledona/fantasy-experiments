## Setup

# for autosklearn
sudo apt-get install swig
sudo /venv/bin/pip install auto-sklearn


# for tpot (as of 2023.3.8)
sudo /venv/bin/pip install torch torchvision torchaudio
sudo /venv/bin/pip install tpot "numpy<1.24"
